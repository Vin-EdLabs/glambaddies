/** Hidden admin panel + API base path (replaces /admin). */
export const ADMIN_PATH = '/glam-baddies'
/** Axios path prefix for admin API routes (`/api` is already on the client baseURL). */
export const ADMIN_API = '/glam-baddies'
