# Legacy note — use nginx-glambaddies.conf instead.
# Production host is https://www.glambaddies.com (API upstream 127.0.0.1:3100).
# This file is kept only as a historical reference and should not be deployed.
